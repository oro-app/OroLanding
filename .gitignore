node_modules
dist
.DS_Store
*.local
.vercel
.seo-server

# Ephemeral build-output dirs from the `verify` skill (never commit)
.verify_build*/

# Vite temp config files leaked when a dev/build process is killed mid-load
vite.config.js.timestamp-*.mjs

.env
.env.local
.env*.local

# Launch film is hosted on YouTube — keep any local video file out of git.
public/film/*.mp4
