// Social-proof figures. Single source of truth — bump these as they grow.
// (Currently surfaced subtly as microcopy under the final CTA; could later
// be wired to a real source like App Store Connect / a Supabase count.)
export const USER_COUNT = 600
