# BUI-621 local frontend draft

## Connected submission flow

The website now saves through `/api/beta-request`, a dedicated Apps Script writer, and a private Google Sheet. See [the setup and rehearsal guide](beta-signup-setup.md) for configuration, deployment, retry behavior, and operator handoff.

Production remains closed by default. A server availability check opens the form only when intake is enabled and its required settings are valid. Missing configuration shows “Invites open soon.” Local development and Vercel preview builds retain the design-only form while saving is disabled. Preview receipts are visibly labeled; real confirmation requires a saved request reference from the writer. URL parameters and browser storage cannot enable server writes.

Based on OroLanding `main` at `21ab734`, using the installed `oro-kit` 0.1.0 foundations and components. The full BUI-621 description, its empty comment thread, related BUI-618, and backend PR #418 were read before implementation. #418 was still open.

Run `npm ci`, then `npm run dev -- --host 127.0.0.1 --port 4621 --strictPort`. Open http://127.0.0.1:4621/beta.

The draft uses the approved introduction with Sunny’s requested sentence removals, the same benefits and disclosures, all 12 question groups in order, native radio and checkbox controls, conditional answers, optional-answer clearing, independent consent values, validation and focus handling. Required defaults follow the ticket's working proposal. Text answers stay in React memory and disappear on reload. When intake is enabled, submission sends them to the website endpoint; they are not logged or written to browser storage. Analytics and cookie controls are disabled on this route.

Sunny's subsequent draft revision places all three consent checkboxes below the request button, checks all three by default, and uses the same support-size text as the review-response note. This supersedes the ticket's original unchecked defaults for this local draft; each checkbox can still be changed independently. The next revision removes the consent Optional labels, compacts their spacing, and moves the review-response sentence above the button. Only an unchecked Terms box disables the request button.

The invitation shows one of six pages at a time in a viewport-height stage. Wheel gestures, swipes, keyboard commands, and one centered forward arrow change pages with sequential fade-and-lift transitions. Trackpad momentum cannot skip through multiple pages. Longer content scrolls inside its page before advancing; reduced-motion preferences switch pages instantly. The extra opening examples, “iMessage beta” label, opening invitation tagline, and numbered chapter labels were removed at Sunny’s request, and the tester name is Oronauts throughout. Every invitation page types its headline and then its supporting text, replaying on each visit without blocking navigation. The revised vision and testing headlines follow Sunny’s copy; the accelerator page has been removed. Beta expectations are a checklist, the final sentence about using Oro again has been removed, and the perks use a compact centered vertical list with no supporting sublines. The separate gratitude page and Oro signoff have been removed. Reduced motion shows all text immediately. The page counter, progress marks, instructions, and visible back arrow have been removed. Scrolling forward from the final invitation page opens the form; scrolling back and keyboard navigation still revisit earlier pages. A persistent skip link opens a question-by-question journey: a name-only opening step, then the 12 original question groups followed by a dedicated invite-and-consent step. Each question is the page headline, with typing, a shared lavender/champagne background, a quiet progress line, a centered forward arrow, and a back control. Related contact fields and outfit-example answers stay together; the larger cards and section navigation are removed. The original question wording and softly animated choice pills remain. The phone helper sentence has been removed, and “Email us” links to sunny@buildingoro.ca. The name is required and retained in memory with the other answers. Question intent, answer values, optionality, and the consent behavior are preserved. Back, next, and browser history preserve answers in memory. Each Continue validates its current step; final submission checks all steps and focuses the earliest invalid answer.

“Request an invite” validates every step, locks the form while saving, and displays a receipt only after the Google writer confirms it. Errors preserve the answers and the retry key. Closed design previews still offer the separate, labeled receipt preview. The approved question order, copy, layout, and consent defaults are preserved.

The route is prerendered for local production-build checks with `noindex`, but excluded from the public route list and sitemap. All three shared homepage signup CTAs now lead to /beta. A narrow development-proxy exception lets the original local Oro logo load without contacting the backend.

Still required for launch: configure the private Sheet, deploy the generated Apps Script files, configure the Vercel server settings and rate-limit rule, and complete the real provider rehearsal in the setup guide. Before inviting selected testers, verify the merged approved-phone `/get-started` flow, backend migrations and beta configuration, and the confirmed iMessage destination. The beta runs September 26–October 1, 2026. Confirm tester-count and selection promises before publication.

Verification commands and the limits of the automated checks are documented in [the setup guide](beta-signup-setup.md). Automated browser checks intercept submissions and do not establish that the live Google integration is configured.
